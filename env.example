# Meeting Transcription Configuration
# Copy this file to .env and fill in your values

# ===========================================
# REQUIRED: Recall.ai API Key
# Get from: https://recall.ai/dashboard
# ===========================================
RECALL_API_KEY=your_recall_api_key_here

# ===========================================
# AUTHENTICATION (Firebase - Auto-configured)
# ===========================================
# These are set automatically by setup.sh
# Users sign in with Google - no configuration needed!

AUTH_PROVIDER=firebase
FIREBASE_PROJECT_ID=your-gcp-project-id
# GOOGLE_CLOUD_PROJECT is also used if FIREBASE_PROJECT_ID not set

# Webhook signature verification (recommended for production)
# Get from Recall.ai dashboard when setting up webhooks
RECALL_WEBHOOK_SECRET=

# ===========================================
# LLM PROVIDER CONFIGURATION
# ===========================================
LLM_PROVIDER=vertex_ai

# --- Vertex AI (Default for GCP - auto-authenticated) ---
# GCP_REGION=us-central1
# VERTEX_AI_MODEL=gemini-3-pro-preview

# --- Azure OpenAI (Alternative) ---
# To use Azure OpenAI instead of Vertex AI:
# 1. Set LLM_PROVIDER=azure_openai
# 2. Get your Azure OpenAI credentials from Azure Portal:
#    - Go to Azure OpenAI resource > Keys and Endpoint
#    - Create a deployment (e.g., gpt-4o)
#
# For local development:
LLM_PROVIDER=azure_openai
AZURE_OPENAI_API_KEY=your_azure_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-12-01-preview
#
# For GCP Cloud Run deployment:
# Run: ./deploy.sh and select "Azure OpenAI" when prompted
# The script will store your Azure credentials in Google Secret Manager

# ===========================================
# PLUGIN CONFIGURATION
# ===========================================
# Control which plugins are loaded at startup

# Enable/disable built-in plugins (Educational, etc.)
ENABLE_BUILTIN_PLUGINS=true

# Enable/disable auto-discovery of custom plugins from plugins/ directory
# ⚠️  SECURITY WARNING: Custom plugins execute arbitrary Python code!
#     Custom plugins have full access to:
#     - All environment variables (API keys, secrets)
#     - File system (read/write/delete files)
#     - Network (make HTTP requests, exfiltrate data)
#     - Application memory and code
#
#     Only enable if:
#     ✓ You wrote the plugins yourself
#     ✓ You fully trust the plugin source
#     ✓ You've reviewed all plugin code
#
#     For production/multi-user deployments, set to false and use only
#     vetted built-in plugins.
ENABLE_PLUGIN_DISCOVERY=true

# Comma-separated list of plugin names to disable
# Uses the plugin's 'name' property (not filename)
# Example: DISABLED_PLUGINS=educational,therapy
DISABLED_PLUGINS=

# ===========================================
# STORAGE CONFIGURATION
# ===========================================
# GCS bucket for storing transcripts and PDFs
OUTPUT_BUCKET=

# Days to retain meetings/files (0 = forever)
RETENTION_DAYS=30

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=8080
DEBUG=false

# ===========================================
# ADVANCED: API Key Authentication
# ===========================================
# For programmatic access (scripts, CI/CD)
# Only use if you understand the security implications
# API_KEY=your_secret_api_key_here

# ===========================================
# DEVELOPMENT ONLY
# ===========================================
# Allow unauthenticated access (NEVER use in production!)
# AUTH_ALLOW_ANONYMOUS=true
